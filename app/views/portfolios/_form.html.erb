	<%= form_for @portfolio do |f|%>
		<%= f.text_field :name, :placeholder => "Portfolio name"%><br>
		<%= f.text_field :type, :placeholder => "Portfolio type"%><br>
		<%= f.submit "Add New Portfolio" %>
	<% end %>